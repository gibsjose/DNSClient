#include <iostream>

int main() {
    //Initialization

    while(1) {
        //Get domain from user
        std::cout << "Enter a domain name: " << std::endl;

        //Connect to server

        //Create a DNS packet

        //Send packet to server

        //Wait for response

        //Print response
    }

    //Close
}
